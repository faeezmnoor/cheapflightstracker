# Secrets / local env
.env
*.env
!.env.example

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local artifacts
*.tmp
email_preview.html

# QA evidence: written every run, uploaded as a CI artifact. Not committed —
# the repo tracks the price history, not each day's rendered email.
artifacts/
observations/
